# Generated by dockerfile_splitter
# ghcr.io/kmmndr/yaroute:latest-test

ARG SOURCE_IMAGE_TAG=latest
ARG SOURCE_IMAGE=ghcr.io/kmmndr/yaroute
FROM $SOURCE_IMAGE:${SOURCE_IMAGE_TAG}-build

RUN bundle config --local without 'development' \
 && bundle install
RUN yarn install --production=false

###
